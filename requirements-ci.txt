# All runtime dependencies EXCEPT hail.
# Hail is excluded because it requires Java, takes ~15 min to install,
# and is only used inside Cohort.by_genotype() which is AoU-only and not tested in CI.
# Install alongside the package using: pip install --no-deps -e . && pip install -r requirements-ci.txt

adjusttext
connectorx
duckdb
google-cloud-bigquery
google-cloud-storage
lifelines>=0.28.0,<0.30.1
lxml
matplotlib
numpy>=1.21.0,<2.0
pandas>=1.3.0
polars
psutil
pyarrow>=10.0.1
statsmodels
tqdm

# test dependencies
pytest
