import os
import polars as pl
import queries
import utils


def get_covariate(natural_age=False,
                  age_at_last_event=False,
                  sex_at_birth=False,
                  genetic_ancestry=False,
                  pc_count=0):
    if natural_age:
        pass
    if age_at_last_event:
        pass
    if sex_at_birth:
        pass
    if genetic_ancestry:
        pass
    if pc_count > 0:
        pass
